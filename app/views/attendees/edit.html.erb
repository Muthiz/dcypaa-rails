<%= content_for :title, 'Edit Attendee #{@attendee.id}' %>
<div class="row">
  <div class="medium-8 medium-offset-2 columns">
    <h1 class="text-center">ATTENDEE <%= @attendee.id %></h1>

    <%= render 'form' %>

    <%= link_to 'Back to Home', root_path, :class => "small radius info button left" %>
  </div>
</div>